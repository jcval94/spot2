name: Spot2 experiments

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - experiment/response-time-random-forest
    paths:
      - "experiments/**"
      - "data/candidate/**"
      - ".github/workflows/lead-attention-experiment.yml"
  pull_request:
    branches:
      - main
    paths:
      - "experiments/**"
      - "data/candidate/**"
      - ".github/workflows/lead-attention-experiment.yml"

permissions:
  contents: read

jobs:
  experiments:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install experiment dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r experiments/requirements.txt

      - name: Run lead attention experiment
        run: python experiments/lead_attention_experiment.py

      - name: Run response-time Random Forest experiment
        run: python experiments/response_time_random_forest/run_experiment.py

      - name: Publish experiment reports in job summary
        if: always()
        run: |
          if [ -f experiments/lead_attention/results/report.md ]; then
            cat experiments/lead_attention/results/report.md >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -f experiments/response_time_random_forest/results/report.md ]; then
            printf "\n\n---\n\n" >> "$GITHUB_STEP_SUMMARY"
            cat experiments/response_time_random_forest/results/report.md >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload experiment results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: spot2-experiments
          path: |
            experiments/lead_attention/results/
            experiments/response_time_random_forest/results/
          if-no-files-found: warn
          retention-days: 30
