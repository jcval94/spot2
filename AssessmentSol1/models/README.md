# Models

Intentionally empty of trained models in PROMPT 0.

Historical model metrics are evidence about failure modes and research direction. Historical model artifacts are never imported.

Model selection begins only after target, splits and ABTs are rebuilt from raw data under AssessmentSol1 contracts.
