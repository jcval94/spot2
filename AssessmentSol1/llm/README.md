# LLM

PROMPT 0 uses historical LLM evidence only to set boundaries.

Current evidence:

- E017: LLM-derived listing semantic features were not incrementally actionable for the current ABT.
- PR #19 live E015: GPT-5 nano was cheap/stable but over-alerted, so it is not an autonomous catalog gate.
- PR #20 E018: deterministic semantic Rules did not pass the LeadQuality Lift@10% promotion gate.

Therefore no LLM feature enters LeadQuality now. LLM use may be reopened when a materially new unstructured source exists (especially raw inquiry text), under Rules-first residual evaluation.
